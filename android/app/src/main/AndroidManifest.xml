<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Notifications : demandée à l'exécution depuis Android 13. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Alarmes exactes. USE_EXACT_ALARM est accordée automatiquement et réservée
      par Google aux applications de réveil, d'agenda et de rappels — ce qui est
      précisément la nature de celle-ci. SCHEDULE_EXACT_ALARM couvre Android 12
      et 12L, où USE_EXACT_ALARM n'existe pas encore.
      Une alarme inexacte peut être différée de plusieurs dizaines de minutes en
      mode Doze, ce qui est inacceptable pour un rappel horaire.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <!-- Réarmement des alarmes après un redémarrage du téléphone. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!--
        Récepteur des alarmes programmées. Non exporté : seul le système, via le
        PendingIntent que nous lui avons confié, doit pouvoir le réveiller.
      -->
      <receiver
          android:name="com.sharpreminder.triggers.datetime.AlarmReceiver"
          android:exported="false" />

      <!--
        Réarmement au démarrage. Exporté car c'est le système qui diffuse
        BOOT_COMPLETED, mais restreint à ces seules actions par son filtre.
      -->
      <receiver
          android:name="com.sharpreminder.triggers.BootReceiver"
          android:exported="true">
        <intent-filter>
          <action android:name="android.intent.action.BOOT_COMPLETED" />
          <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
        </intent-filter>
      </receiver>
    </application>
</manifest>
